import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: '#be1d38',
        },
        secondary: {
            main: '#ffffff',
        },
    },
})

export default theme